// In canAccessTestType:
if (subscription && subscription.endDate >= Date.now()) {
  const isPaid = subscription.amount > 0;
  if (isPaid) {
    return { canAccess: true, reason: "paid_subscription" };
  }
}
