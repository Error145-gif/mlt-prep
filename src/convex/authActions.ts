// This file is no longer needed - Convex Auth handles OAuth internally
// Keeping file for potential future custom auth actions
import { internalAction } from "./_generated/server";

// Placeholder for future custom auth actions
export const placeholder = internalAction({
  args: {},
  handler: async () => {
    return null;
  },
});